function truncateString(str, num) {
    let newStr = '';
    if (str.length > num) {
        newStr += str.substr(0, num) + '...';
    } else if (str.length == num) {
        newStr = str;
    } else {
        return str;
    }
    return newStr;
}
