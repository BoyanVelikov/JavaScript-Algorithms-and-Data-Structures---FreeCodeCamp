function truthCheck(collection, pre) {

    let midArr = collection.filter(x => x[pre]);
    if (midArr.length == collection.length) {
        return true;
    } else {
        return false;
    }

}
